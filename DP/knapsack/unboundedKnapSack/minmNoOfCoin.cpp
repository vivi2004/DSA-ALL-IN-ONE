#include <bits/stdc++.h>
using namespace std;
int minNoOfCoin(vector<int> &coin, int sum)
{
    // Base.. case..
    int n = coin.size();
    vector<vector<int>> dp(n + 1, vector<int>(sum + 1, 0));
    for (int i = 0; i <= n; i++)
    {
        for (int j = 0; j <= sum; j++)
        {
            if ()
        }
    }
}

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}
